Project instructions live in [`AGENTS.md`](AGENTS.md) — that file is the single
source of truth. Pi loads it automatically (it prefers `AGENTS.md` over
`CLAUDE.md`); this stub exists only so tools that read `CLAUDE.md` by name are
pointed at the real file instead of silently getting nothing.
